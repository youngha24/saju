# 사주 ✦ 운세 — 휴대폰 설치 배포 안내 (2026-07-06)

## 1. 결론 먼저

| 전달 방법 | 아이폰 | 갤럭시 | 판정 |
|---|---|---|---|
| **이메일 첨부(HTML) → 탭** | ❌ 미리보기만 뜸 (iOS가 첨부 HTML의 자바스크립트를 차단 — 계산 전부 불가) | △ 다운로드 후 Chrome으로 열면 동작하나 "설치"는 안 됨(바로가기 수준) | 부적합 |
| **카톡 파일 전송 → 탭** | ❌ 카톡 내 미리보기(자바스크립트 차단) | △ "다른 앱으로 열기 → Chrome" 하면 동작. 단, 매번 파일로 열어야 하고 앱 설치 아님 | 임시 열람용만 가능 |
| **인터넷 주소(링크) 전송 → 탭** | ✅ Safari에서 열림 → 홈 화면 추가 = 앱 아이콘 | ✅ Chrome이 **"앱 설치"** 배너 표시 = 진짜 설치 | ★ 정답 |

첨부파일 방식은 애플·카카오가 보안상 스크립트를 막아 두어 **기술적으로 불가능**합니다.
대신 이 폴더(배포_GitHubPages)를 무료 웹주소에 올리면, **문자·카톡·이메일로 링크만 보내서 탭 → 설치**가 됩니다. 한 번 설치하면 오프라인(비행기 모드)에서도 동작하도록 서비스워커까지 넣어 두었습니다.

## 2. 이 폴더 구성 (수정 없이 그대로 올리면 됨)

- index.html — 앱 본체 (v1.0-r11, 매니페스트·서비스워커 연결 완료)
- manifest.webmanifest — 설치 정보(이름·아이콘·세로 고정·단독 실행)
- sw.js — 오프라인 캐시
- icon-192/512/180.png — 설치 아이콘

## 3. GitHub Pages 무료 배포 (1회, 약 5분)

1. github.com 가입/로그인 → 우측 상단 **+** → **New repository**
2. 이름 예: `saju` → **Public** → Create
3. **uploading an existing file** 클릭 → 이 폴더의 파일 6개 전부 끌어다 놓기 → Commit
4. 저장소 **Settings → Pages** → Branch: `main` / 폴더 `/ (root)` → Save
5. 1~2분 후 주소 발급: `https://아이디.github.io/saju/`

## 4. 가족에게 보내는 방법

- 카톡/문자/이메일에 위 주소만 붙여 보내면 끝.
- **아이폰**: 링크 탭 → Safari → 공유(⬆) → **홈 화면에 추가** → 아이콘 생김
  (카톡에서 열렸으면 우하단 ⋯ → "Safari로 열기" 후 진행)
- **갤럭시**: 링크 탭 → Chrome → 상단 알림 또는 메뉴(⋮) → **앱 설치 / 홈 화면에 추가**
  (카톡에서 열렸으면 우상단 ⋮ → "다른 브라우저로 열기")

## 5. 갱신 방법

앱을 고치면 새 `사주모바일_클로드_*.html`을 `index.html`로 이름 바꿔 저장소에 다시 업로드(덮어쓰기)하고, sw.js의 `CACHE="saju-v1"` 숫자를 v2, v3…로 올리면 사용자 폰이 자동 갱신됩니다.

※ GitHub 계정 정보가 있으면 업로드까지 대신 진행해 드릴 수 있습니다(또는 Netlify Drop처럼 끌어다 놓기만 하면 되는 무료 서비스도 있습니다).
